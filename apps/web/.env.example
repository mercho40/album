# Local: http://localhost:3000
# Producción: URL del back desplegado en Dokploy/EC2 (ej. https://api.album.tudominio.com)
PUBLIC_API_URL=http://localhost:3000

# Generar con: openssl rand -base64 32. Debe coincidir con el del back.
BETTER_AUTH_SECRET=
