# Local: http://localhost:3000 (back corriendo en otro puerto)
# Producción: /api (rewrite a album-back.vercel.app vía vercel.json)
PUBLIC_API_URL=http://localhost:3000

# Generar con: openssl rand -base64 32. Debe coincidir con el del back.
BETTER_AUTH_SECRET=
