{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "sveltekit",
  "buildCommand": "cd ../.. && bun install && cd apps/web && bun run build",
  "installCommand": "echo 'install hecho en buildCommand'",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://album-back-two.vercel.app/:path*" }
  ]
}
